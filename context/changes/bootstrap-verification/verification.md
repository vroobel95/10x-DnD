---
bootstrapped_at: 2026-05-19T00:00:00Z
starter_id: 10x-astro-starter
starter_name: "10x Astro Starter (Astro + Supabase + Cloudflare)"
project_name: dnd-5enemy
language_family: js
package_manager: npm
cwd_strategy: git-clone
bootstrapper_confidence: first-class
phase_3_status: ok
audit_command: "npm audit --json"
---

## Hand-off

```yaml
---
starter_id: 10x-astro-starter
package_manager: npm
project_name: dnd-5enemy
hints:
  language_family: js
  team_size: solo
  deployment_target: cloudflare-pages
  ci_provider: github-actions
  ci_default_flow: auto-deploy-on-merge
  bootstrapper_confidence: first-class
  path_taken: standard
  quality_override: false
  self_check_answers: null
  has_auth: true
  has_payments: false
  has_realtime: false
  has_ai: true
  has_background_jobs: false
---
```

### Why this stack

DnD 5enemy is a 3-week, after-hours web app for a solo GM tool — exactly the profile the 10x Astro Starter was designed for. Astro 6 + React 19 handles the UI (enemy cards, battle views), Supabase provides auth (email + password or OAuth, matching the PRD's access control requirement) and a PostgreSQL database for persisting confirmed enemies and battles, and Cloudflare Pages gives edge deployment with minimal ops overhead. TypeScript end-to-end and Zod schemas at boundaries keep the codebase agent-navigable, which matters given the AI generation core. The short timeline and after-hours constraint favor a batteries-included starter over assembling a custom stack — Supabase auth and database are pre-wired, so the first working deploy can happen in hours, not days.

## Pre-scaffold verification

| Signal      | Value                                                     | Severity | Notes                                                   |
| ----------- | --------------------------------------------------------- | -------- | ------------------------------------------------------- |
| npm package | not run                                                   | n/a      | cmd_template starts with `git clone`; npm check skipped |
| GitHub repo | przeprogramowani/10x-astro-starter last pushed 2026-05-17 | fresh    | from card.docs_url; checked via GitHub REST API         |

## Scaffold log

**Resolved invocation**: `git clone https://github.com/przeprogramowani/10x-astro-starter .bootstrap-scaffold && cd .bootstrap-scaffold && npm install`  
**Strategy**: git-clone (clone starter repo without keeping its git history)  
**Exit code**: 0  
**Git history stripped**: `.bootstrap-scaffold/.git/` deleted before move-up  
**Files moved**: all scaffold contents (root files, .github/workflows/ci.yml, .husky/, .vscode/, node_modules/, plus targeted recovery of src/, public/, supabase/)  
**Conflicts (.scaffold siblings)**: none  
**.gitignore handling**: moved silently (no .gitignore existed in cwd)  
**.bootstrap-scaffold cleanup**: deleted

**Note**: During move-up, the `src/`, `public/`, and `supabase/` directories were recovered via a targeted shallow clone after a partial-run issue with the conflict matrix traversal over node_modules. All files are present in cwd; no data loss.

## Post-scaffold audit

**Tool**: `npm audit --json`  
**Summary**: 0 CRITICAL, 1 HIGH, 10 MODERATE, 0 LOW  
**Direct vs transitive**: not distinguished by this tool (npm audit does not split direct/transitive in JSON output)

#### CRITICAL findings

None.

#### HIGH findings

**devalue** (versions 5.6.3–5.8.0)  
Advisory: [GHSA-77vg-94rm-hx3p](https://github.com/advisories/GHSA-77vg-94rm-hx3p)  
Description: Svelte devalue — DoS via sparse array deserialization. Maliciously crafted input can cause excessive memory allocation on deserialization.  
Transitive via: Astro dependencies  
Fix: run `npm audit fix` once the upstream patch propagates; no breaking changes expected.

#### MODERATE findings

10 moderate vulnerabilities logged (not surfaced inline per severity policy). Affected packages include `ws` (uninitialized memory disclosure) and `yaml` (stack overflow), among others. Run `npm audit` for full details.

#### LOW / INFO findings

None.

## Hints recorded but not acted on

The following hints from the hand-off were read and preserved in this log but no automated action was taken in v1:

- `hints.team_size: solo` — informational; weighted the stack selection but bootstrapper takes no action.
- `hints.deployment_target: cloudflare-pages` — no CI/CD scaffolding in v1; `wrangler.jsonc` is included by the starter's scaffold.
- `hints.ci_provider: github-actions` — no workflow file generation in v1; `.github/workflows/ci.yml` was included by the starter's scaffold (not generated by bootstrapper).
- `hints.ci_default_flow: auto-deploy-on-merge` — informational; no automated CI/CD wiring in v1.
- `hints.quality_override: false` — no action (standard path; all quality gates passed).
- `hints.self_check_answers: null` — standard path taken; self-check not run.
- `hints.has_auth: true` — Supabase auth is pre-wired in the starter; no additional scaffolding.
- `hints.has_ai: true` — no AI/LLM scaffolding in v1; integration is the developer's responsibility.
- `hints.has_payments: false` — no action.
- `hints.has_realtime: false` — no action.
- `hints.has_background_jobs: false` — no action.
- `hints.bootstrapper_confidence: first-class` — surfaced in conversation; no compensating action in v1.
- `AGENTS.md` / `CLAUDE.md` generation — deferred to future M1L4 skill ("Memory Architecture").

## Next steps

Your project is scaffolded and verified — happy hacking.

A future skill will set up agent context (`CLAUDE.md`, `AGENTS.md`) with stack-specific conventions, compensating instructions for the AI generation feature, and Supabase/Cloudflare integration patterns. That is the next link in the chain (M1L4, not yet available).

One item to track before shipping: the **HIGH** `devalue` advisory (GHSA-77vg-94rm-hx3p). Run `npm audit fix` once the upstream Astro patch lands — no manual intervention needed until then.
