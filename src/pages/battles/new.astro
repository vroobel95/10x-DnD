---
import Layout from "@/layouts/Layout.astro";
import CreateBattleForm from "@/components/battles/CreateBattleForm";

const campaignId = Astro.url.searchParams.get("campaignId");

if (!campaignId) {
  return Astro.redirect(`/campaigns?error=${encodeURIComponent("Select a campaign to create a battle")}`);
}

const error = Astro.url.searchParams.get("error");
---

<Layout title="New Battle">
  <div class="bg-cosmic flex min-h-screen items-center justify-center p-4">
    <div class="w-full max-w-sm rounded-2xl border border-white/10 bg-white/10 p-8 text-white backdrop-blur-xl">
      <h1
        class="mb-6 bg-gradient-to-r from-blue-200 to-purple-200 bg-clip-text text-center text-2xl font-bold text-transparent"
      >
        New Battle
      </h1>
      <CreateBattleForm campaignId={campaignId} serverError={error} client:load />
      <p class="mt-4 text-center text-sm text-blue-100/60">
        <a href={`/campaigns/${campaignId}`} class="text-purple-300 hover:underline">Back to campaign</a>
      </p>
    </div>
  </div>
</Layout>
